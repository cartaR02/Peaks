import { StyleSheet } from 'react-native';

export const textStyles = StyleSheet.create({
  textStyles: {
    fontSize: 19,
    color: '#C800FF',
    fontWeight: '600',
    fontFamily: 'Verdana',
    textAlign: 'center',
  },
});